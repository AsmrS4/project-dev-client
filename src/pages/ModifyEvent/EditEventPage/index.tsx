import React from 'react';
import styles from '../Page.module.scss';
const EditEventPage = () => {
    return (
        <section className={styles.eventPage}>
            <div className={styles.eventHeader}>Редактор мероприятия</div>
        </section>
    );
};

export default EditEventPage;
